# UI Prototype

The original AuraCall UI prototype files are here for reference.
These demonstrate the visual design that the Next.js app implements.

Open `index.html` in a browser to see the interactive prototype.
